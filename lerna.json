{
  "npmClient": "yarn",
  "packages": ["libs/*"],
  "version": "independent",
  "ignoreChanges": ["apps/**", "integration/**", "**/*.md"],
  "command": {
    "version": {
      "conventionalCommits": true,
      "allowBranch": ["master"],
      "push": false,
      "gitTagVersion": false
    }
  }
}
