import React from 'react';

import './app.scss';
import { MyComponent } from '@stencil-toolkit-nx/react-lib';

export const App = () => {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./${fileName}.${style} file.
   */
  return <MyComponent first="Stencil" last="'Don't call me a framework' JS"></MyComponent>;
};

export default App;
